# Phase-Two Frontend Evidence

This file is the non-sensitive evidence index for the authenticated frontend work. The implementation baseline below uses only disposable synthetic accounts in an isolated candidate. Production signed-in verification remains a separate, explicitly authorized release gate. Never record credentials, cookies, tokens, private URLs, production row contents, or personal data.

## Baseline Identity

| Field | Value |
| --- | --- |
| Base commit | `ecb7c2e3ae22686ffbcf0fac4e7a1ba98aeafcb9` |
| Official comparison | `v0.1.162` / `27f094e0960ebd8e52de7ff7e763c6fec2ff4057` |
| Candidate commit/image | Embedded commit `f532ecc0518f83a8e78ea39f5e8ae56336e7ae89`; local linux/amd64 `coocode-sub2api:p1-keys-f532ecc`, image ID `sha256:4482f82cedf928fc8cb3288ce80d2d98d53356f1beec7589c9ef599e0c57d3c4`; embedded version `ui-phase2-p1-f532ecc`, build time `2026-07-22T08:44:36Z` |
| Candidate health URL/result | Loopback `http://127.0.0.1:18080/health` -> `{"status":"ok"}`; application, disposable PostgreSQL, and disposable Redis all healthy with restart count `0`; only the application was recreated at `2026-07-22T08:50:49Z`, while PostgreSQL/Redis retained their `2026-07-22T04:38:43Z` starts |
| Synthetic user/admin fixture IDs | `candidate-user` and `candidate-admin`; credentials exist only in the active browser runtime and are neither printed nor written |
| Candidate configuration | Standard mode; `backend_mode=false`, Payment/Risk Control/Available Channels/Affiliate/TOTP/custom menu disabled, Channel Monitor enabled; no production configuration was copied |
| Baseline window | Candidate started `2026-07-22T07:01:19Z`; authenticated browser capture started `2026-07-22T07:08:18Z` |
| Production baseline authorization/time | Pending; candidate evidence does not satisfy production signed-in verification |

## Route And Feature Baseline

For each authenticated route, add one row or a link to its grouped rows.

| Role | Route | Standard/simple/backend result | Sidebar visibility vs direct-route guard | Feature source/state | Safe interactions | Candidate commit/time | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| User | `/dashboard`, `/keys`, `/usage`, `/profile`, `/monitor`, `/subscriptions`, `/redeem` | Standard mode rendered; simple/backend modes were not enabled because changing candidate configuration is outside this read-only baseline | Core entries follow the current sidebar rules; direct routes remained reachable | Public settings; Channel Monitor on, other listed routes unflagged | Navigate, scroll, open non-mutating UI only | `ecb7c2e`, `2026-07-22T07:08Z+` | Reachable; no document overflow at 390 in the grouped route sweep |
| User | `/available-channels` | Standard mode rendered | Direct route reachable although the feature is absent from the sidebar when disabled | Public `available_channels=false` | Read-only | Same | Reachable; no document overflow |
| User | `/affiliate` | Standard mode rendered | Direct route reachable although the feature is absent from the sidebar when disabled | Public `affiliate=false` | Read-only and copy-button visibility only; copy not required for baseline | Same | Reachable; Important 390 px overflow recorded as `UI-P3U-AFF-001` |
| User | `/batch-image` and alias `/docs/batch-image` | Standard mode rendered | Direct route reachable while current eligibility keeps it out of the sidebar | Eligibility/account-group source left unchanged | Read-only | Same | Reachable; deferred feature surface |
| User | `/purchase`, `/orders`, `/payment/qrcode` | Standard mode | Guard redirects direct navigation to `/dashboard`; sidebar entries hidden | Public `payment=false` | Navigation only | Same | Expected guarded redirect |
| User | `/custom/nonexistent` | Standard mode | Direct route remains on the custom route | Public custom menu count `0` | Read-only | Same | Missing-page state rendered; deferred |
| Admin | `/admin/dashboard`, `/admin/users`, `/admin/groups`, `/admin/channels/pricing`, `/admin/channels/monitor`, `/admin/subscriptions`, `/admin/accounts`, `/admin/announcements`, `/admin/proxies`, `/admin/redeem`, `/admin/promo-codes`, `/admin/settings`, `/admin/usage`, `/admin/audit-logs` | Standard mode rendered; simple/backend modes not enabled | Direct routes reachable under administrator guard; sidebar filtering left unchanged | Existing public/admin sources; Channel Monitor on | Navigate, scroll, open/close safe dialogs only | Same | Reachable; no grouped 390 px document overflow; Users dialog issue recorded separately |
| Admin | `/admin/ops` | Standard mode rendered | Direct route reachable | Existing Ops source left unchanged | Read-only | Same | Important 390 px overflow and very long page recorded as `UI-DEFER-OPS-001` |
| Admin | `/admin/risk-control`, `/admin/prompt-audit` | Standard mode | Guard redirects direct navigation to `/admin/settings` | Public `risk_control=false` | Navigation only | Same | Expected guarded redirect; feature surfaces not opened |
| Admin | `/admin/affiliates/invites`, `/admin/affiliates/rebates`, `/admin/affiliates/transfers` | Standard mode rendered | Direct routes reachable although affiliate navigation is disabled | Public `affiliate=false` | Read-only | Same | Reachable; deferred P3-admin surfaces |
| Admin | `/admin/orders/dashboard`, `/admin/orders`, `/admin/orders/plans` | Standard mode | Guard redirects direct navigation to `/admin/dashboard` | Public `payment=false` | Navigation only | Same | Expected guarded redirect |

## Issue-To-Batch Acceptance Matrix

Use stable IDs (`UI-P0A-001`, `UI-P0B-001`, `UI-P1-001`, and so on). Before/after rows must use the same role, fixture/data shape, feature state, and viewport.

| Issue ID | Severity | Role/route | Feature/fixture state | Viewport | Baseline observation | Owning batch | Frozen files | Observable acceptance | Before screenshot | After screenshot | Disposition |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `UI-P0B-001` | Important | Admin `/admin/users`, edit-user `BaseDialog`; global shared primitive | Synthetic admin/user; candidate standard-mode feature state recorded in Baseline Identity | 390 x 844 | Dialog bounds `left=8`, `right=382`, `width=374`; only 8 px viewport inset, below the 16 px mobile target | P0B | `frontend/src/style.css`; new focused `frontend/src/components/common/__tests__/BaseDialog.spec.ts` | `left >= 16`, `right <= innerWidth - 16`, `width <= innerWidth - 32`, document contained; body/footer non-overlap; Escape, focus restoration, body lock, close events unchanged | Captured in the active candidate browser; not committed because fixture identifiers are visible | Exact `bfc3889` candidate capture; not committed | Closed: 390 px panel `left=16`, `right=374`, `width=358`; 768/1440 contained and behavior preserved |
| `UI-P0B-002` | Important | Admin `/admin/ops`, Alert Rules outer `BaseDialog`; global shared body-lock contract | Synthetic admin; Ops enabled; nested editor and confirmation dialogs initially hidden | 390 x 844 | On exact candidate `62ff557`, one visible overlay/dialog had `body.modal-open=false`, computed body `overflow-y=visible`, and background document height `33,964` vs viewport `844`; hidden nested dialogs unconditionally removed the outer lock | P0B expanded checkpoint | `frontend/src/components/common/BaseDialog.vue`; existing focused `BaseDialog.spec.ts` | Hidden dialog mount cannot release another owner; with multiple open dialogs, closing/unmounting one retains lock; only the last owner unlocks; Ops visible dialog has `modal-open=true`, `overflow-y=hidden`, and unchanged background scroll position until close | Active browser capture, not committed | Exact `bfc3889` candidate capture; not committed | Closed: one visible dialog retained `modal-open=true` and `overflow-y=hidden`; PageDown left background `scrollY=0`; close restored overflow and trigger focus |
| `UI-P1-KEYS-001` | Important | User `/keys`, empty fixture plus one disposable key for interaction-only checks | Synthetic user; current visible-column preference; disposable key removed before the window closed | 768 x 900 | Nine-column header compresses to wrapper width and Chinese labels wrap character by character; no useful wrapper scroll range | P1 | `frontend/src/views/user/KeysView.vue`; focused `KeysView.spec.ts` update | Headers do not break per character; table wrapper alone may scroll horizontally; document remains contained; sorting/columns/pagination/actions unchanged | Active browser capture, not committed | Exact `f532ecc` candidate capture; not committed | Closed: nine headers are single-line `nowrap`; wrapper `708/841` with 133 px range and successful horizontal scroll; document `768/768`; interactions and cleanup passed |
| `UI-P1-DASH-001` | Minor | User `/dashboard`, spend statistics | Synthetic user, empty/default totals | 390 x 844 | The total-cost standard-price span splits into two line fragments, leaving `/` alone at the end of the actual-price line and `$0.0000` on the next line | P1 | `frontend/src/components/user/dashboard/UserDashboardStats.vue`; new focused `frontend/src/components/user/dashboard/__tests__/UserDashboardStats.spec.ts` | Separator plus standard price is one indivisible inline unit and may move together to the next line; actual price may remain on the prior line; no document overflow; values/tooltips unchanged | Active browser capture, not committed | Pending | Accepted Minor after Keys Important closed; whole-pair nowrap explicitly rejected because the 390 px content column is only 85 px wide |
| `UI-P3U-AFF-001` | Important | User `/affiliate` | Synthetic user; Affiliate disabled in sidebar but direct route currently reachable; long generated link | 390 x 844 | `documentElement` about `542/382`; invite-code/link flex rows expand to about 501 px and copy buttons leave the viewport | P3-user | `frontend/src/views/user/AffiliateView.vue`; new focused `AffiliateView.spec.ts` | Document scroll width equals client width; both rows stay in content bounds; original copy behavior and direct-route semantics unchanged | Active browser capture, not committed | Pending | Accepted for P3-user; do not pull across batch ordering |
| `UI-DEFER-OPS-001` | Important | Admin `/admin/ops` | Synthetic admin; current Ops settings/data | 390 x 844 | `documentElement` about `596/382`; fixed-width filter row is about 516 px; page height about 33,228 px | Separate deferred Ops change | Not frozen | No document overflow; controls remain reachable; page density/virtualization require separate contract | Active browser capture, not committed | Pending | Deferred; not owned by P0B/P1/P2/P3 |

## Geometry And Browser Results

| Batch | Role/route | Viewport | Document client/scroll width | Table wrapper client/scroll width | Dialog bounds | Header/filter/table-header bounds | Console/API window and allowlist | Screenshot | Result |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| P0A baseline | User/admin shell | 390/768/1440 plus 1023/1024 edge | No unexplained shell-level document overflow; 1023 mobile drawer/full-width main, 1024 fixed 256 px sidebar/about 760 px main | Representative table checks below | N/A | Sidebar/main/header offsets agree at the `lg` edge | No new console/API errors in shell sweep | Active browser capture, not committed | No-op approved by read-only source/test audit |
| P0B baseline | Admin `/admin/users`, edit dialog | 390 x 844 | Document remained contained | N/A | `left=8`, `right=382`, `width=374`, `top=21`, `bottom=823`; footer `top=768`, `bottom=810` | Body/footer visual crowding noted but overlap not proven; only inset is accepted for this batch | No new error tied to opening/closing dialog | Active browser capture, not committed | Fails 16 px inset target |
| P0B candidate `62ff557` | Admin `/admin/ops`, Alert Rules dialog | 390 x 844 | Background about `382/596`; document height `33,964` | N/A | Visible dialog `left=16`, width about `350`; one overlay/dialog | Background body lock was removed by hidden nested dialog: class absent and computed overflow visible | No new console/API error tied to open/close | Active browser capture, not committed | `UI-P0B-002`; stop and renew scope |
| P0B exact candidate `bfc3889` | Admin `/admin/ops`, Alert Rules dialog | 390 x 844 | Existing deferred Ops page remains about `390/596`; dialog overlay itself is viewport-bound | N/A | Panel `left=16`, `right=374`, `width=358`; one visible dialog; body/footer non-overlap | `modal-open=true`, body `overflow-y=hidden`; PageDown kept background `scrollY=0`; Escape restored overflow and focus to the Alert Rules trigger | Zero warning/error browser logs in the full final window | Active browser capture, not committed | `UI-P0B-002` closed; deferred Ops page overflow remains outside P0B |
| P0B exact candidate `bfc3889` | Admin `/admin/users`, edit dialog | 390/768/1440 | `382/382`, `768/768`, `1432/1432` before dialog; contained at all widths | 767 remains card mode; 768 remains table mode with wide table inside its wrapper | 390 `16..374` / width 358; 768 `128..640` / width 512; 1440 `464..976` / width 512 | Body lock true; body/footer non-overlap; Escape restored focus; no contract change | Zero warning/error browser logs in the full final window | Active browser capture, not committed | `UI-P0B-001` closed; 767/768 breakpoint behavior preserved |
| P0B exact candidate `bfc3889` | Admin `/admin/audit-logs`, detail dialog | 390 x 844 | `390/390` while open | N/A | `left=16`, `right=374`, `width=358`; scrollable body `739/908` | Body lock true while open and false after Escape | Zero warning/error browser logs in the full final window | Active browser capture, not committed | Representative shared-consumer pass |
| P0B exact candidate `bfc3889` | User `/batch-image`, usage guide | 390/768/1440 | `390/390`, `768/768`, `1432/1432` before dialog; contained while open | N/A | Width 358 / 736 / 896; 16 px mobile inset | Body lock true; body/footer non-overlap; Escape left zero dialogs/locks | Zero warning/error browser logs in the full final window | Active browser capture, not committed | Genuine user-role representative pass |
| P0B feature-off regression | Admin Prompt Audit and Payment; user Payment | 390 x 844 | All destination pages contained | N/A | No dialog opened | `/admin/prompt-audit` -> `/admin/settings`; admin Payment -> `/admin/dashboard`; user `/purchase` -> `/dashboard` | Zero warning/error browser logs in the full final window | N/A | Guard semantics preserved |
| P0B baseline | Admin `/admin/users` | 767 x 900 | `767/767`; document contained | Card mode at 767 | N/A | Breakpoint switches before the table layout | No new error | Active browser capture, not committed | Contained |
| P0B baseline | Admin `/admin/users` | 768 x 900 | `768/768`; document contained | Actual table about 1368 px inside an explicit wrapper; wrapper-only horizontal scrolling | N/A | Table mode at 768 | No new error | Active browser capture, not committed | Contained |
| P1 baseline | User `/keys` | 768 x 900 | `760/760`; document contained | About `708/708`; nine columns compress instead of producing useful wrapper scroll | N/A | Header labels wrap per character | No new error | Active browser capture, not committed | `UI-P1-KEYS-001` |
| P1 Keys pre-change recheck on `bfc3889` | User `/keys` | 768 x 900 | `768/768`; document contained | Wrapper/table `708/708`; horizontal range `0` | N/A | Nine header cells use `white-space: normal`; header row height `72.5` px | No new error | Active browser capture, not committed | Reproduced `UI-P1-KEYS-001` on the exact P0B candidate |
| P1 Keys exact candidate `f532ecc` | User `/keys` | 768 x 900 | `768/768`; document contained before and after wrapper scrolling | Wrapper `708/841`; range `133`; programmatic scroll reached `121`; table width `841` | N/A | All nine headers use `white-space: nowrap`; header row height `52.5` px and each measured header content height `20` px | Browser warnings/errors `0`; server-side sort changed `aria-sort` from `none` to `ascending`; 11-item column menu opened/closed | Active browser capture, not committed | `UI-P1-KEYS-001` closed; scrolling belongs to the wrapper only |
| P1 Keys exact candidate `f532ecc` | User `/keys` | 390 x 844 | `390/390`; no document overflow | Mobile card/empty-state mode; no visible table | N/A | Header table intentionally absent below the desktop breakpoint | Browser warnings/errors `0`; filters, refresh, column settings, and create entry remained in bounds | Active browser capture, not committed | Responsive mode preserved |
| P1 Keys exact candidate `f532ecc` | User `/keys` | 1440 x 900 | `1432/1432`; no document overflow | Wrapper/table `1098/1098`; no unnecessary horizontal range | N/A | All nine headers remain single-line at `52.5` px height | Browser warnings/errors `0` | Active browser capture, not committed | Wide layout preserved |
| P1 Keys interaction fixture `f532ecc` | User `/keys` | 768 x 900 | `768/768` throughout | One disposable candidate-only key produced one data row and a one-page paginator; both returned to empty after deletion | Edit dialog opened with body lock and closed without a remaining dialog/lock | Copy, group, use, import, disable, edit, and delete actions were reachable; previous/next were correctly disabled on page 1; edit opened/closed; delete cleanup removed the row and paginator | Browser warnings/errors `0`; no key value was read, printed, or persisted in evidence | Active browser capture, not committed | Interaction and cleanup pass; candidate fixture restored |
| P1 baseline | User `/dashboard` | 390 / 768 / 1440 | `382/382`, `760/760`, `1432/1432`; heights about `2173`, `2051`, `1178` | N/A | N/A | At 390 the price content column is 85 px; the total standard span has one 7.4 px `/` fragment on the actual line and one 44.9 px price fragment below; 768/1440 do not reproduce | No new error | Active browser capture, not committed | `UI-P1-DASH-001`; exact evidence rejects whole-pair nowrap |
| P3-user baseline | User `/affiliate` | 390 x 844 | About `382/542` | N/A | N/A | Code/link flex rows about 501 px | No new error | Active browser capture, not committed | `UI-P3U-AFF-001` |
| Deferred baseline | Admin `/admin/ops` | 390 x 844 | About `382/596`; height about 33,228 px | N/A | N/A | Filter row about 516 px | No new error | Active browser capture, not committed | `UI-DEFER-OPS-001` |

Baseline warning: on the initial Admin Channels load around `2026-07-22T07:10Z`, `GET /api/v1/admin/settings/web-search-emulation` returned one `404`; immediate later requests returned `200`. Candidate logs contain no related `5xx`, fatal, or panic. This is an unowned upstream-baseline warning, not an approved release allowlist; every modified-batch rerun must prove it is not newly introduced and the release gate remains closed if it becomes reproducible or unexplained.

Existing errors may be allowlisted only with timestamp, route, source, reproduction, owner, and proof that the batch did not introduce them. Every release requires zero new or unowned console errors, unhandled rejections, and failed API requests.

## Frozen Source Scope

Record the output or artifact reference for:

```text
git diff --name-status ecb7c2e3ae22686ffbcf0fac4e7a1ba98aeafcb9...HEAD -- frontend/
git diff --stat 27f094e0960ebd8e52de7ff7e763c6fec2ff4057...HEAD -- <each-frozen-file>
```

Global dialog consumer inventory is reproducible with:

```text
rg -l '<(BaseDialog|ConfirmDialog)\b' frontend/src -g '*.vue' | wc -l
rg -o '<(BaseDialog|ConfirmDialog)\b' frontend/src -g '*.vue' | wc -l
```

Baseline result: 80 Vue files and 132 tag instances. The mandatory high-risk representative set is Audit (`AuditLogView.vue`), Prompt Audit (`PromptAuditView.vue` plus dialog components), Batch Image (`BatchImageGuideView.vue`), Ops (`OpsDashboard.vue` plus dialog components), and Payment (`PaymentQRDialog.vue`, `AdminRefundDialog.vue`). These pages are for safe open/scroll/close/Escape checks only; no confirm, save, clear, delete, probe, payment, refund, or callback action is authorized.

| Batch | Accepted issue IDs | Frozen files | Shared consumers | New upstream-owned file? | Reviewer decision |
| --- | --- | --- | --- | --- | --- |
| P0A | None; baseline found no Important shell issue | None; freeze `AppLayout.vue`, `AppSidebar.vue`, `AppHeader.vue`, `TablePageLayout.vue`, shell store/router semantics | User/admin shell and all authenticated routes | No | No-op approved; any later shell edit requires renewed review |
| P0B | `UI-P0B-001`, `UI-P0B-002` | `frontend/src/style.css`; `frontend/src/components/common/BaseDialog.vue`; `frontend/src/components/common/__tests__/BaseDialog.spec.ts` | All `BaseDialog` and `ConfirmDialog` consumers; representative Audit Log, Prompt Audit, Batch Image, Ops, and Payment checks required | No new upstream-owned source file; modify two existing files and add one local focused test | `APPROVE_EXPANDED_SCOPE`; 0 Critical, 1 accepted Important (`UI-P0B-002`), 0 Minor |
| P1 Keys | `UI-P1-KEYS-001` | `frontend/src/views/user/KeysView.vue`; existing `frontend/src/views/user/__tests__/KeysView.spec.ts` | User API-key table only; shared `DataTable` and `TablePageLayout` remain frozen | No; use the existing `Column.class` contract and existing test file | `APPROVE_SCOPE` and `APPROVE_IMPLEMENTATION`; 0 Critical, 0 Important; test-name Minor fixed before candidate |
| P1 Dashboard | `UI-P1-DASH-001` | `frontend/src/components/user/dashboard/UserDashboardStats.vue`; new `frontend/src/components/user/dashboard/__tests__/UserDashboardStats.spec.ts` | User `/dashboard` only; parent view, grid, scripts, requests, stores, router, and shared components remain frozen | One new local focused test only | `APPROVE_SCOPE`; 0 Critical, 1 acceptance-wording Important fixed before source edit, 0 Minor |

## Commands And Results

| Batch | Focused Vitest paths/result | lint/typecheck/full Vitest/build | dependency/OpenSpec checks | Exact candidate start/seed/health/teardown | Logs/health/restarts | Result |
| --- | --- | --- | --- | --- | --- | --- |
| Baseline/P0A | `AppSidebar.spec.ts`, `TablePageLayout.spec.ts`, `app.spec.ts`: 3 files / 37 tests passed | Not yet run for P0B | `openspec validate ... --strict` passed before execution; rerun pending after evidence update | `/private/tmp/coocode-v0162-candidate.compose.yml`; `docker compose -p coocode-v0162-candidate -f /private/tmp/coocode-v0162-candidate.compose.yml up -d --wait --no-deps sub2api`; candidate uses disposable data services and runtime-only synthetic credentials | App/PostgreSQL/Redis healthy, all restart `0`; loopback health `{"status":"ok"}` | Baseline ready; candidate remains running for same-state after checks |
| P0B pre-candidate | New `BaseDialog.spec.ts`: 1 file / 4 tests; P0A + Prompt Audit + Payment representative set: 7 files / 59 tests; all passed | ESLint passed; `vue-tsc --noEmit` passed; full Vitest 186 files / 1,257 tests passed; production build passed with existing Browserslist, dynamic-import, and chunk-size warnings | npm audit: 0 high / 0 critical; repository exceptions validated; strict OpenSpec and `git diff --check` passed | Pending exact-commit image build and isolated candidate replacement | Pending post-replacement health/log/restart window | Static/unit/build gate passed |
| P0B candidate checkpoint | Same pre-candidate gates | Docker pnpm 9 frozen-lockfile build passed | Image `coocode-sub2api:p0b-62ff557`, linux/amd64 ID `sha256:cd4dd53713c592020a2a8129419c2cb0f4e4339533446a6cff6ba3ab09180931`; embedded commit `62ff557223257f677a7aa4bb009150e1aec01af3` | Replaced only isolated candidate `sub2api`; PostgreSQL/Redis start times unchanged | App/data services healthy, restart `0`, health OK; browser closed `UI-P0B-001` but reproduced `UI-P0B-002` | Failed expanded P0B body-lock gate; no push/release |
| P0B expanded implementation | `BaseDialog.spec.ts`: 1 file / 6 tests; representative shared-consumer set: 7 files / 61 tests; all passed | ESLint passed; `vue-tsc --noEmit` passed; full Vitest 186 files / 1,259 tests passed; production build passed with the same existing Browserslist, dynamic-import, and chunk-size warnings | Strict OpenSpec and `git diff --check` passed | Pending second exact-commit image and isolated candidate replacement | Pending post-replacement health/log/restart and browser checks | Static/unit/build gate passed; read-only code review approved with 0 Critical / 0 Important / 0 Minor |
| P0B exact candidate `bfc3889` | Same expanded focused/full gates | Docker pnpm 9 frozen-lockfile linux/amd64 build passed; embedded commit/version/time independently read from the running container | Image ID `sha256:d5f5a5faea0e301b4e5f0a1d917393bb4e921d6f14923cf17019aa03cb9eb726`; strict OpenSpec and diff check rerun after evidence update | Recreated only isolated candidate `sub2api`; PostgreSQL/Redis starts unchanged; loopback health returned `{"status":"ok"}` | All three services healthy/restart `0`; final browser warnings/errors `0`; 352 application-log lines with 4xx `0`, 5xx/panic/fatal `0` | Both accepted Important issues closed; final read-only review `APPROVE_P0B` |
| P1 Keys pre-candidate | `KeysView.spec.ts`: 1 file / 10 tests passed | ESLint passed; `vue-tsc --noEmit` passed; full Vitest 186 files / 1,260 tests passed; production build passed with the same existing warnings | Dependency manifests/lockfile unchanged; strict OpenSpec and `git diff --check` rerun after this evidence update | Pending exact-commit image and isolated application replacement | Pending browser geometry, interaction, health, restart, and log window | Static/unit/build gate passed; implementation review approved |
| P1 Keys exact candidate `f532ecc` | Same focused/full/static gates | Docker pnpm 9 frozen-lockfile linux/amd64 build passed; running binary reported exact commit `f532ecc0518f83a8e78ea39f5e8ae56336e7ae89`, version `ui-phase2-p1-f532ecc`, build time `2026-07-22T08:44:36Z` | Image ID `sha256:4482f82cedf928fc8cb3288ce80d2d98d53356f1beec7589c9ef599e0c57d3c4`; strict OpenSpec and diff check rerun after this evidence update | Recreated only isolated candidate `sub2api`; PostgreSQL/Redis starts unchanged; loopback health returned `{"status":"ok"}`; disposable browser key removed | All three services healthy/restart `0`; browser warnings/errors `0`; 130-line interaction-window log with 4xx/5xx/panic/fatal `0` | Important `UI-P1-KEYS-001` closed; final read-only review `APPROVE_P1_KEYS` |
| P1 Dashboard pre-candidate | New `UserDashboardStats.spec.ts`: 1 file / 1 test passed | ESLint passed; `vue-tsc --noEmit` passed; full Vitest 187 files / 1,261 tests passed; production build passed with the same existing Browserslist, dynamic-import, and chunk-size warnings | Dependency manifests/lockfile unchanged; strict OpenSpec and `git diff --check` passed | Pending exact-commit image and isolated application replacement | Pending 390/768/1440 line-fragment geometry, browser errors, health, restart, and log window | Static/unit/build gate passed; independent implementation review `APPROVE_IMPLEMENTATION` |

The first literal `pnpm exec vitest` attempt did not run tests: the host pnpm 11 wrapper rejected the existing pnpm-managed module directory and attempted a network reinstall. No dependency install or lockfile change was accepted. Verification then used the repository's already-installed exact binaries (`node_modules/.bin/vitest`, `eslint`, `vue-tsc`, and `vite`); the Docker candidate build remains pinned to pnpm 9 with `--frozen-lockfile`.

The exact candidate record includes sanitized compose/env artifact paths, image tag/digest/embedded commit, loopback URL, disposable PostgreSQL/Redis identity, synthetic role aliases, literal start command, fixture seed command, health body, and the teardown command/plan. Actual teardown remains pending while the same isolated candidate is retained for subsequent phase-two checks. Secret values are omitted.

## Independent Reviews

| Batch | Review input commit/diff | Validation evidence | Critical | Important | Minor | Decision |
| --- | --- | --- | --- | --- | --- | --- |
| Plan revision | This OpenSpec change | Strict validation and diff check passed; two independent read-only reviewers approved | 0 open | 0 open | 2 wording/status findings fixed | `APPROVE_PLAN` |
| P0A baseline | `09c7ffecf51fd801a0ca1638e2db81693e37c4ac` plus read-only source/test audit | 1023/1024 browser geometry; 3 files / 37 focused tests passed | 0 | 0 | Existing tests are not sufficient protection for a future shared-shell edit | `NO_OP`; shared shell frozen |
| P0B scope gate | Baseline evidence, issue/file matrix, 80-file/132-instance consumer inventory, and frozen two-file code scope | Strict OpenSpec and diff check passed; route/alias/upstream wording findings fixed and re-reviewed | 0 | 0 | Multi-dialog body-lock risk is pre-existing and unproven; stop for renewed review only if candidate reproduction occurs | `APPROVE_SCOPE` |
| P0B expanded scope gate | Candidate `62ff557` Ops Alert Rules browser reproduction plus `BaseDialog.vue`/nested consumer source | One visible dialog, body lock absent, computed overflow visible, 33,964 px background; independent read-only review | 0 | 1 accepted | 0 | `APPROVE_EXPANDED_SCOPE`; add owner-safe lock only, no Ops change |
| P0B expanded implementation review | Current four-file uncommitted diff against `62ff557` | 6 focused dialog tests; 7 files / 61 representative tests; ESLint, typecheck, 186 files / 1,259 full tests, build, strict OpenSpec, and diff check all passed | 0 | 0 | 0 | `APPROVE`; candidate Ops/browser evidence still required |
| P0B final evidence review | Frontend `09c7ffecf..bfc38893c` plus current `evidence.md`/`tasks.md` diff | Focused/representative/full/static gates; exact candidate identity; both-role browser geometry; zero browser errors; health/restart and 352-line server-log window | 0 | 0 | 0 | `APPROVE_P0B`; teardown 8.4 and production section 9 remain open |
| P1 Keys scope review | `UI-P1-KEYS-001` baseline plus `KeysView`/`DataTable`/layout/test source | Exact 768 px wrapper/table metrics and existing `Column.class`/wrapper overflow contract | 0 | 0 | 1 recommendation | `APPROVE_SCOPE`; merge nowrap through filtered columns and use browser for real geometry |
| P1 Keys implementation review | Current two-file Keys diff | 1 file / 10 focused tests; ESLint, typecheck, 186 files / 1,260 full tests, build, and diff check passed | 0 | 0 | 1 fixed | `APPROVE_IMPLEMENTATION`; renamed the unit test so it does not claim browser layout evidence |
| P1 Keys final evidence review | Commit `f532ecc0518f83a8e78ea39f5e8ae56336e7ae89` plus current `evidence.md` diff | Exact candidate identity; 390/768/1440 browser geometry; sorting/columns/pagination/actions and disposable-data cleanup; zero browser/server errors; health/restarts | 0 | 0 | 0 | `APPROVE_P1_KEYS`; broad P1, teardown, and production gates remain open |
| P1 Dashboard scope review | Exact 390 px line fragments plus `UserDashboardStats.vue`, its sole page caller, and existing test patterns | Content width `85`; total standard span splits into slash `7.4` px and price `44.9` px fragments; official comparison has no existing local component diff | 0 | 1 fixed | 0 | `APPROVE_SCOPE`; only the separator-plus-standard span may be made an inline no-break unit |
| P1 Dashboard implementation review | Current component/test/evidence diff | Focused 1/1; ESLint, typecheck, 187 files / 1,261 full tests, build, strict OpenSpec, and diff check passed | 0 | 0 | 0 | `APPROVE_IMPLEMENTATION`; exact candidate browser evidence remains required |

## Release And Rollback

| Batch | Exact commit | CI run | OCI index / amd64 digest | Previous production image | Backup path/check | Deploy command/result | Smoke/log/browser result | Rollback/post-rollback health |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Pending | Pending | Pending | Pending | Pending | Pending | Pending | Pending | Pending |

The authoritative procedure is the `Production Preflight And Cutover` section of `openspec/changes/upgrade-sub2api-v0-1-162/evidence.md`. Documentation-only commits do not authorize or trigger a production replacement.
