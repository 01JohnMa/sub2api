# Upgrade Evidence

## 2026-07-22 Baseline

- Source branch: `codex/upgrade-sub2api-v0-1-162` in isolated worktree `/private/tmp/coocode-upgrade-v0162`.
- Coococode source: `db290d77d3a97f4776129e0c181196b8c873462b`.
- Official tag: `v0.1.162` -> `27f094e0960ebd8e52de7ff7e763c6fec2ff4057`.
- Merge base: `c32e29bab0046b0588528a815e0d602f5336b3d6`.
- Unique commits: coococode 8, official tag 1252; all eight coococode patches remain patch-unique.
- Main worktree contained one tracked modification and multiple untracked user-owned paths. It was not modified by the upgrade setup.
- Read-only merge preview identified 18 content conflicts. The real `--no-commit` merge additionally surfaced the expected upstream i18n module split as two modify/delete conflicts, for 20 unresolved paths total.

## Merge Conflict Set

- Security/build/dependency: `.github/audit-exceptions.yml`, `Dockerfile`, `deploy/Dockerfile`, `frontend/index.html`, `frontend/package.json`, `frontend/pnpm-lock.yaml`.
- Shared frontend and tests: `AccountUsageCell.vue`, admin `UsageTable.vue`, `AppHeader.vue`, `AppSidebar.vue`, `AuthLayout.vue`, `TablePageLayout.vue`, `TablePageLayout.spec.ts`, and `billingMode.ts`.
- Pages and auth: `HomeView.vue`, `EmailVerifyView.vue`, `EmailVerifyView.spec.ts`, and user `UsageView.vue`.
- i18n migration: old monolithic `locales/en.ts` and `locales/zh.ts` were deleted upstream and must not be restored; coococode keys will be migrated into the new locale modules.

## Production Baseline

- Running image reference: `ghcr.io/01johnma/sub2api:ui-redesign-db290d7@sha256:a4f858815d821ff4eb760b4249e720da85e6783aa7f0baae69a797e4d7a4f4fa`.
- Container state: healthy, restart count 0; origin and public `/health` returned `{"status":"ok"}`.
- Public settings reported version `ui-redesign-db290d7`; CPA service was active.
- Current rollback backup directory: `/opt/sub2api/backups/20260721T063141Z-pre-security-ui`.
- Database: 211 applied migration rows; no invalid indexes.
- Migrations 151 through 172 are already applied in production. All 27 stored checksums match the corresponding `v0.1.162` migration content.
- Pending tag migrations begin at 173 and comprise 17 SQL files through 184.
- Relevant relation sizes: `ops_system_logs` about 360 MB, `usage_logs` about 98 MB, `scheduler_outbox` about 2.4 MB; the remaining checked relations were below 1 MB.
- `SETUP_MIGRATION_TIMEOUT_SECONDS` is not set; the tagged default is 60 seconds.
- Production explicitly stores `api_key_acl_trust_forwarded_ip=false`. Independent review found the tagged one-time compatibility migration could still rewrite that stored value to `true` when no migration marker or trusted-proxy policy existed. The integrated source now treats every stored value as operator-controlled and never rewrites an explicit `false`; a focused regression test covers the production-shaped state.
- Nginx sets `X-Real-IP` from the direct peer and appends `X-Forwarded-For`; no trusted real-IP directives were found in the active filtered configuration. Forwarded-header trust must remain disabled for this upgrade.

## Pre-Merge Verification

- Focused frontend Vitest: 9 files, 38 tests passed for brand assets, home, login accessibility, responsive table layout, account/admin usage components, and user/admin usage views.
- Focused backend packages passed: `./internal/service`, `./internal/handler`, and `./internal/handler/admin`.
- Frontend production build passed and generated the embedded `backend/internal/web/dist` output. Existing dynamic-import and chunk-size warnings were recorded but did not fail the build.
- Static Vite output and Go embed paths are consistent.

## Stable Tag Merge

- `git merge --no-commit --no-ff v0.1.162` was run only in the isolated upgrade worktree.
- Automatic merge stopped at the documented 20 paths; all conflicts were resolved and staged, but no merge commit was created.
- `git diff --name-only --diff-filter=U` and `git ls-files -u` both report zero unresolved entries.
- Build resolution keeps the tagged BuildKit/cross-architecture/docs/legal/version-resolution behavior while retaining the coococode title, favicon, vendored SheetJS 0.20.3 tarball, and checksum verification.
- The lockfile was regenerated by pnpm 9.15.9 rather than edited manually. A clean worktree-local `pnpm 9.15.9 install --frozen-lockfile --prefer-offline` installed 968 packages without changing the lockfile.
- App shell and public/auth resolution retain coococode branding while adopting tagged URL sanitization, frozen-balance details, batch-image permissions, audit navigation, sidebar scroll restoration, and pending OAuth/adoption/invite/referral handling.
- The tagged modular locale layout remains authoritative; no deleted monolithic locale file was restored. Independent review and candidate-browser evidence found missing shared-control accessibility labels and Sub2API onboarding copy; those retained brand/auth strings were moved into the tagged locale modules and protected with a real-bundle regression test.
- Usage and account-display resolution adopts the tagged platform, quota, image-input, video-billing, filtering, sorting, chart, error-request, and CSV behavior. The coococode tablet/mobile local table-overflow containment remains covered by the merged layout test.
- Exact conflict-marker scans over all resolution paths found no remaining markers.

## Tagged Migration And Runtime Risks

- Pending migrations add usage/account metadata, long-context billing defaults, audit logs, prompt-audit storage, ingress aggregates, and auth-cache invalidation triggers.
- Prompt audit remains disabled by default and must not be enabled as part of this upgrade.
- Admin audit logging and retention behavior must be included in post-migration verification.
- The candidate must verify pending concurrent indexes with an extended migration timeout and confirm no invalid indexes afterward.

## Resolved Source Verification

- A clean worktree-local pnpm 9.15.9 install completed from the merged lockfile with 968 packages and no lockfile drift.
- Frontend `lint:check`, `typecheck`, and production build passed. The build transformed 949 modules and emitted the Go-embedded web distribution; inherited dynamic/static import and chunk-size warnings remain non-fatal.
- The first complete frontend test run exposed three contract mismatches. Two tagged rollback API assertions omitted the tagged 15-minute request timeout and were corrected. The page-size mismatch was resolved in favor of the existing coococode contract from `dcc071c90`: a user's persisted page size continues to win over the configured default.
- After the fixes, the complete frontend suite passed twice. Following independent review and candidate-browser remediation, the final rerun passed 185 files and 1253 tests, including the real-locale, responsive version-badge, and usage-tooltip contracts; focused rollback and persisted-page-size tests also passed 4 of 4.
- Online pnpm audit reported 0 high and 0 critical production vulnerabilities, with 8 low and 30 moderate findings. The repository's exact exception checker passed after obsolete, expired lodash/lodash-es/axios exceptions were removed. This is not a claim of zero vulnerabilities.
- Backend `go test ./... -count=1`, `go vet ./...`, and a CGO-disabled server build passed after dependency remediation. After the forwarded-IP migration fix, the complete hidden `-tags=unit` suite also passed outside the filesystem sandbox; its initial sandbox run failed only where test servers attempted prohibited loopback binds.
- The first online `govulncheck` found reachable `GO-2026-5970` in `golang.org/x/text` 0.37.0. The module was raised to the minimum fixed 0.39.0; Go minimal-version selection required the coordinated `x/crypto`, `x/mod`, `x/net`, `x/sync`, `x/sys`, `x/term`, and `x/tools` versions recorded in `go.mod`. After `go mod tidy`, the backend suite and build passed and `govulncheck` reported zero reachable vulnerabilities.
- Docker-backed integration tests started disposable PostgreSQL 18.1 and Redis 8.4 containers, applied the full migration set to an empty database, and passed migration concurrency locking, idempotency/schema, migration 175 long-context billing invariants, and migration 184 auth-cache invalidation trigger checks. Testcontainers removed the disposable environment.
- The independent custom-patch audit found the eight coococode commits preserved or intentionally superseded by tagged equivalents. It identified the page-size semantic merge issue before staging; the preserving fix is now staged.
- The final independent review then found one Critical and three Important issues: a forwarded-IP migration could reverse a stored safety choice, shared auth accessibility locale keys were missing, onboarding branding had regressed, and the documented manual workflow path was not executable while the workflow was absent from the default branch. The source, tests, locale bundles, workflow trigger, and this evidence were corrected. A stale write-failure unit expectation was also updated to the fail-closed contract; focused forwarded-IP tests, the full `-tags=unit` suite, the full frontend suite, lint, typecheck, build, production-dependency audit/exception check, and `govulncheck` all passed afterward.

## Release Path Decision

- This customized branch will not use the tagged GoReleaser `release.yml` workflow. Its manual-dispatch frontend checkout is not pinned to the selected tag and therefore cannot prove a single-source frontend/backend image for this integration.
- The repository-specific `coococode-image.yml` builds the root Dockerfile from the workflow ref and stamps `COMMIT=${{ github.sha }}`. Because GitHub only permits manual dispatch when the workflow exists on the default branch, this upgrade branch is explicitly included in the workflow's `push` trigger. Release must push the reviewed merge commit, verify the resulting workflow SHA equals that commit, and record the immutable GHCR digest before production.
- The upstream compose template's local `sub2api:latest` image and relaxed URL defaults are not the production deployment source. Candidate and production checks must keep the current explicit GHCR image and the existing production URL/client-IP security settings.

## Isolated Candidate And Browser Verification

- The final source candidate was built from staged fingerprint `8bd0fedbb7e7eda8eb1ebb34f2245d70e27b29a5c74e559cf0e6b7dd4db4521e` as local tag `coocode-v0162-candidate:8bd0fedb`; image ID is `sha256:d5fd8ca685a27048a3e06bad75ec2ef4e2c1994f5eeeb597e4222794a6a0824a`, platform is `linux/amd64`, and the embedded version is `v0.1.162-coococode-pre-8bd0fedb`.
- The isolated stack bound the application only to `127.0.0.1:18080` and used disposable candidate PostgreSQL 18.1 and Redis 8.4 services. The application and both dependencies were healthy; `/health` returned `{"status":"ok"}`.
- The candidate database contains 228 applied migration rows, including migration 184. The production-shaped stored value `api_key_acl_trust_forwarded_ip=false` remained false after startup, `forwarded_client_ip_mode_v2_migrated=true`, no `prompt_audit_config` row was created, no invalid indexes exist, and only the two local synthetic browser-test users are present.
- Final-image logs from `2026-07-22T04:59:31Z` through the completed browser pass contained 205 lines and zero matching 4xx/5xx status, error/fatal level, or panic events.
- Public/auth and signed-in flows were exercised with local synthetic credentials. Login, password visibility labeling, language switching, and the reopened administrator onboarding dialog all worked; onboarding rendered `欢迎使用 coococode` rather than the upstream product name.
- Administrator routes covered dashboard, users, usage, settings, and the responsive sidebar at 390, 768, and 1440 px. User routes covered dashboard, keys, usage, subscriptions, and profile across the same viewport set. The prompt-audit route redirected to settings while risk control was disabled, which matches the default-off contract.
- At 390 px the administrator usage page reported document `clientWidth=382` and `scrollWidth=382`; at 768 px the users page reported `768/768`; at 1440 px the dashboard reported `1432/1432`. The equivalent user checks reported `382/382`, `768/768`, and `1432/1432`. No right-edge offenders or global horizontal overflow remained.
- Browser inspection exposed two upgrade-specific responsive defects before acceptance. `VersionBadge.vue` now truncates long release labels inside the mobile sidebar, preserves the full value in `title`, and avoids a doubled leading `v`; its open dropdown remained within the 382 px document. `UsageStatsCards.vue` now right-aligns the hidden cache-token tooltip on small screens and restores centered positioning from `sm` upward. Focused component tests protect both fixes.
- The final browser pass produced no console entries after the final candidate container start. The complete frontend Vitest suite, lint check, typecheck, and production build were rerun after both responsive fixes and passed.
- The final independent read-only review found zero Critical and zero Important findings and approved entry into the commit/push gate. Its only non-blocking Minor notes that the version-prefix regression is currently protected by source assertions plus real-browser evidence rather than a mounted DOM assertion; that test-strengthening item is deferred to the separate phase-two frontend change.
